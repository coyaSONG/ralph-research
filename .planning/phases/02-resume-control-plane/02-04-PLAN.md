---
phase: 02-resume-control-plane
plan: "04"
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - src/adapters/fs/lockfile.ts
  - src/app/services/run-cycle-service.ts
  - tests/lockfile-workspace-manager.test.ts
  - tests/run-cycle-service.test.ts
autonomous: true
requirements:
  - RECV-04
must_haves:
  truths:
    - "Healthy long-running work keeps its lease alive instead of going stale on a fixed TTL."
    - "A takeover requires TTL expiry plus a grace window, not one missed heartbeat."
    - "When another process owns an active lease, the run path fails immediately with actionable owner details."
  artifacts:
    - path: src/adapters/fs/lockfile.ts
      provides: "Renewable lock lease with heartbeat, grace window, and owner metadata."
    - path: src/app/services/run-cycle-service.ts
      provides: "Run-path adoption of renewable lock ownership and actionable contention errors."
    - path: tests/lockfile-workspace-manager.test.ts
      provides: "Lease-renewal and takeover regression coverage."
    - path: tests/run-cycle-service.test.ts
      provides: "Service-level coverage for active-owner failure behavior."
  key_links:
    - "Lock metadata renewal is tied to active work through a service-owned heartbeat lifecycle."
    - "Takeover checks use ttl + grace before allowing replacement."
    - "Active-owner errors include the runId, pid, and heartbeat age when available."
---

<objective>
Replace the fixed-TTL lock with a renewable lease so long-running healthy work does not look stale, and make lock contention fail fast with actionable owner details instead of silent waiting or premature takeover.

Purpose: Fulfill Phase 2's safety requirement for active ownership without dragging in Phase 3 repair tooling or manual-review semantic changes.
Output: A renewable lock lease, run-service adoption of that lease, and regression coverage for renewal, grace-window takeover, and active-owner errors.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-resume-control-plane/02-CONTEXT.md
@.planning/phases/02-resume-control-plane/02-RESEARCH.md
@.planning/phases/02-resume-control-plane/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/adapters/fs/lockfile.ts
@src/app/services/run-cycle-service.ts
@tests/lockfile-workspace-manager.test.ts
@tests/run-cycle-service.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lease-renewal and takeover tests for the run lock</name>
  <files>tests/lockfile-workspace-manager.test.ts
tests/run-cycle-service.test.ts</files>
  <read_first>.planning/phases/02-resume-control-plane/02-CONTEXT.md
.planning/phases/02-resume-control-plane/02-VALIDATION.md
tests/lockfile-workspace-manager.test.ts
tests/run-cycle-service.test.ts
src/adapters/fs/lockfile.ts
src/app/services/run-cycle-service.ts</read_first>
  <behavior>
    - Test 1: A held lease updates heartbeat metadata before TTL expiry and remains non-stale.
    - Test 2: A takeover is rejected until `ttl + grace` has elapsed or the owner is definitely gone.
    - Test 3: An active-owner failure message contains actionable owner details including runId, pid, and heartbeat age.
    - Test 4: Token-mismatch renewal or release is rejected.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'heartbeat|grace|token mismatch|runId|pid' tests/lockfile-workspace-manager.test.ts tests/run-cycle-service.test.ts` returns matches for all four behaviors.
    - `rg -n 'LockAcquisitionError|Active lock' tests/run-cycle-service.test.ts` returns an assertion for the actionable active-owner failure path.
  </acceptance_criteria>
  <action>Extend `tests/lockfile-workspace-manager.test.ts` and `tests/run-cycle-service.test.ts` with failing coverage for D-11, D-12, and D-13 before changing the lock implementation. The tests must cover heartbeat renewal, grace-window takeover rules, token-mismatch protection, and the run-service error path that surfaces active owner details immediately. Do not add `--wait` polling behavior or generic retry loops.</action>
  <verify>
    <automated>npx vitest run tests/lockfile-workspace-manager.test.ts tests/run-cycle-service.test.ts</automated>
  </verify>
  <done>Failing tests exist for renewable leases, grace-window stale detection, token protection, and actionable active-owner failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement renewable lease ownership on the run path</name>
  <files>src/adapters/fs/lockfile.ts
src/app/services/run-cycle-service.ts
tests/lockfile-workspace-manager.test.ts
tests/run-cycle-service.test.ts</files>
  <read_first>src/adapters/fs/lockfile.ts
src/app/services/run-cycle-service.ts
tests/lockfile-workspace-manager.test.ts
tests/run-cycle-service.test.ts
.planning/phases/02-resume-control-plane/02-RESEARCH.md</read_first>
  <behavior>
    - Test 1: Service-owned lease renewal starts before long-running work and stops cleanly on success or failure.
    - Test 2: The lock adapter requires ttl plus grace before marking an owner stale.
    - Test 3: Active lock contention returns an immediate error with owner details rather than waiting.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'heartbeat|graceMs|renew|updatedAt' src/adapters/fs/lockfile.ts` returns matches showing renewable lease support.
    - `rg -n 'runId|pid|heartbeat age|active lease' src/app/services/run-cycle-service.ts` returns matches for actionable contention messaging.
    - `rg -n -- '--wait' src/adapters/fs/lockfile.ts src/app/services/run-cycle-service.ts tests/lockfile-workspace-manager.test.ts tests/run-cycle-service.test.ts` returns no matches.
  </acceptance_criteria>
  <action>Refactor `src/adapters/fs/lockfile.ts` into a renewable lease with heartbeat refresh, explicit grace-window stale detection, and token-checked renewal/release per D-11 and D-12. Update `RunCycleService` to acquire the lease with owner metadata that includes the active run context, renew it while the cycle executes, and surface immediate failure details per D-13 when another healthy owner holds the lease. Keep the behavior fail-fast and local-first; do not add wait/retry modes, do not broaden the lock scope into manual-review semantics, and do not introduce Phase 3 repair commands.</action>
  <verify>
    <automated>npx vitest run tests/lockfile-workspace-manager.test.ts tests/run-cycle-service.test.ts</automated>
  </verify>
  <done>The run path holds a renewable lease through long work, stale takeover requires ttl plus grace, and contention errors are immediate and actionable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| lock metadata file -> process ownership decisions | filesystem metadata controls whether a second process may take over active work |
| service runtime -> lease heartbeat lifecycle | missed renewal or incorrect release can let another process steal a healthy run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | D | `src/adapters/fs/lockfile.ts` | mitigate | Renew lease metadata during active work and require ttl plus grace before a takeover is allowed. |
| T-02-11 | T | `src/adapters/fs/lockfile.ts` | mitigate | Keep token-checked renewal and release so one process cannot accidentally clear another process's lease. |
| T-02-12 | R | `src/app/services/run-cycle-service.ts` | mitigate | Include actionable owner metadata in active-lease failures so the operator can distinguish live contention from stale debris. |
</threat_model>

<verification>
Run `npx vitest run tests/lockfile-workspace-manager.test.ts tests/run-cycle-service.test.ts` after each task, then rerun `npx vitest run tests/state-engines.test.ts tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts tests/run-command.test.ts` before closing the plan.
</verification>

<success_criteria>
- Long-running healthy work no longer loses its lock simply because five minutes elapsed.
- Stale takeover waits through a grace window and does not steal a live owner on the first missed heartbeat.
- The operator sees who owns the active lease and why the new run was refused.
</success_criteria>

<output>
After completion, create `.planning/phases/02-resume-control-plane/02-resume-control-plane-04-SUMMARY.md`
</output>
