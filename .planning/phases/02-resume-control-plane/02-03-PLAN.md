---
phase: 02-resume-control-plane
plan: "03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/app/services/project-state-service.ts
  - src/cli/commands/status.ts
  - src/cli/commands/inspect.ts
  - src/mcp/server.ts
  - tests/project-state-service.test.ts
  - tests/mcp-server.test.ts
  - tests/status-inspect-command.test.ts
autonomous: true
requirements:
  - RECV-03
must_haves:
  truths:
    - "Status and inspect expose one shared recovery classification and next action."
    - "Inspect includes classification, nextAction, reason, and resumeAllowed in a dedicated recovery section."
    - "CLI and MCP serialize the same recovery truth instead of transport-specific labels."
  artifacts:
    - path: src/app/services/project-state-service.ts
      provides: "Shared recovery read model for status and inspect."
    - path: src/mcp/server.ts
      provides: "MCP parity for recovery truth on run/status responses."
    - path: tests/project-state-service.test.ts
      provides: "Wave 0 regression coverage for recovery classifications."
    - path: tests/mcp-server.test.ts
      provides: "Wave 0 coverage for MCP payload parity."
    - path: tests/status-inspect-command.test.ts
      provides: "CLI verification for status/inspect recovery output."
  key_links:
    - "project-state-service consumes the shared recovery classifier from Plan 02-01."
    - "status, inspect, and MCP read from the same recovery payload shape."
    - "reason strings stay truthful without introducing Phase 3 repair evidence taxonomies."
---

<objective>
Project the Phase 2 recovery truth consistently through read-only surfaces: `status`, `inspect`, and MCP should all explain whether the latest run is idle, resumable, manual-review-blocked, or repair-required, and what the operator should do next.

Purpose: Make recovery state inspectable and trustworthy without inventing new commands or richer repair tooling.
Output: A shared recovery read model plus CLI and MCP parity coverage.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-resume-control-plane/02-CONTEXT.md
@.planning/phases/02-resume-control-plane/02-RESEARCH.md
@.planning/phases/02-resume-control-plane/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@src/app/services/project-state-service.ts
@src/cli/commands/status.ts
@src/cli/commands/inspect.ts
@src/mcp/server.ts
@tests/cli-commands.test.ts

<interfaces>
From src/app/services/project-state-service.ts:
```typescript
export interface ProjectStatus {
  manifestPath: string;
  latestRun: RunRecord | null;
  frontier: FrontierEntry[];
  pendingHumanRuns: RunRecord[];
  decisions: DecisionRecord[];
}

export interface InspectRunResult {
  manifestPath: string;
  run: RunRecord;
  decision: DecisionRecord | null;
  frontier: FrontierEntry[];
  explainability: { ... };
}
```

From src/mcp/server.ts:
```typescript
server.registerTool("get_research_status", ...);
server.registerTool("run_research_cycle", ...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 0: Create the missing recovery read-model test files</name>
  <files>tests/project-state-service.test.ts
tests/mcp-server.test.ts
tests/status-inspect-command.test.ts</files>
  <read_first>.planning/phases/02-resume-control-plane/02-VALIDATION.md
.planning/codebase/TESTING.md
tests/cli-commands.test.ts
src/app/services/project-state-service.ts
src/mcp/server.ts
src/cli/commands/status.ts
src/cli/commands/inspect.ts</read_first>
  <behavior>
    - Test 1: Project status maps no-run, resumable, manual-review-blocked, and repair-required cases onto stable recovery output.
    - Test 2: Inspect adds a `recovery` section with `classification`, `nextAction`, `reason`, and `resumeAllowed`.
    - Test 3: MCP `get_research_status` and `run_research_cycle` surfaces expose the same recovery truth as the service layer.
  </behavior>
  <acceptance_criteria>
    - `test -f tests/project-state-service.test.ts` succeeds.
    - `test -f tests/mcp-server.test.ts` succeeds.
    - `test -f tests/status-inspect-command.test.ts` succeeds.
    - `rg -n 'manual_review_blocked|repair_required|resumeAllowed|nextAction' tests/project-state-service.test.ts tests/mcp-server.test.ts tests/status-inspect-command.test.ts` returns matches.
  </acceptance_criteria>
  <action>Create the Wave 0 test files called out by `02-VALIDATION.md` and add one dedicated CLI read-model test file so this plan can verify RECV-03 without colliding with the run-path tests in Plan 02-02. Follow existing temp-repo integration patterns from `tests/cli-commands.test.ts`, keep the tests Node-only and file-backed, and lock in D-05, D-06, D-07, and D-08 through explicit recovery payload assertions. Do not introduce repair evidence taxonomies or manual-review mutation semantics.</action>
  <verify>
    <automated>npx vitest run tests/project-state-service.test.ts tests/mcp-server.test.ts tests/status-inspect-command.test.ts</automated>
  </verify>
  <done>Missing Wave 0 tests exist and fail against the current recovery read-model surfaces.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: Implement one shared recovery read model for status, inspect, and MCP</name>
  <files>src/app/services/project-state-service.ts
src/cli/commands/status.ts
src/cli/commands/inspect.ts
src/mcp/server.ts
tests/project-state-service.test.ts
tests/mcp-server.test.ts
tests/status-inspect-command.test.ts</files>
  <read_first>src/core/state/recovery-classifier.ts
src/app/services/project-state-service.ts
src/cli/commands/status.ts
src/cli/commands/inspect.ts
src/mcp/server.ts
tests/project-state-service.test.ts
tests/mcp-server.test.ts
tests/status-inspect-command.test.ts</read_first>
  <behavior>
    - Test 1: `status` shows classification plus nextAction in both text and JSON output.
    - Test 2: `inspect` returns a dedicated `recovery` section with the four required fields.
    - Test 3: MCP run/status payloads do not invent a different recovery vocabulary from the CLI/service layer.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'recovery' src/app/services/project-state-service.ts src/cli/commands/inspect.ts tests/project-state-service.test.ts` returns matches.
    - `rg -n 'classification|nextAction|reason|resumeAllowed' src/app/services/project-state-service.ts src/cli/commands/status.ts src/cli/commands/inspect.ts src/mcp/server.ts` returns matches.
    - `rg -n 'resume_required' src/app/services/project-state-service.ts src/cli/commands/status.ts src/cli/commands/inspect.ts src/mcp/server.ts` returns no matches.
  </acceptance_criteria>
  <action>Extend `project-state-service.ts` to compute a shared recovery payload from the classifier introduced in Plan 02-01 and thread it into `getProjectStatus()` and `inspectRun()` per D-05 and D-06. Update `status`, `inspect`, and `src/mcp/server.ts` so they serialize that shared payload directly, keeping `status` concise with classification plus next action while `inspect` exposes the full recovery section. Keep the reason string truthful but lightweight per D-07, classify ambiguous partials as `repair_required` per D-08, and do not add Phase 3 repair evidence schemas or Phase 4 manual-review mutation behavior.</action>
  <verify>
    <automated>npx vitest run tests/project-state-service.test.ts tests/mcp-server.test.ts tests/status-inspect-command.test.ts</automated>
  </verify>
  <done>Read-only CLI and MCP surfaces agree on one shared recovery model, and the new tests prove the four classifications plus next-action reporting.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted state -> status/inspect read model | operator-visible recovery truth is derived from stored run/decision/frontier data |
| service payload -> CLI and MCP serialization | transport drift can mislead users into taking the wrong recovery action |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | R | `src/app/services/project-state-service.ts` | mitigate | Centralize recovery projection in the service layer and test the same payload across CLI and MCP. |
| T-02-08 | I | `src/cli/commands/inspect.ts` | accept | Recovery reasons remain local-repo operational strings; no new sensitive data source is introduced in this phase. |
| T-02-09 | D | `src/mcp/server.ts` | mitigate | Reuse the service payload instead of transport-specific logic so MCP cannot drift from CLI recovery truth. |
</threat_model>

<verification>
Run `npx vitest run tests/project-state-service.test.ts tests/mcp-server.test.ts tests/status-inspect-command.test.ts` after each task, then rerun the same command plus `tests/state-engines.test.ts` before handing off.
</verification>

<success_criteria>
- `status` answers "what should I do next?" with classification plus nextAction.
- `inspect` contains a dedicated recovery section with the exact required fields.
- MCP recovery semantics match the service/CLI layer instead of exposing `resume_required` or another transport-only label.
</success_criteria>

<output>
After completion, create `.planning/phases/02-resume-control-plane/02-resume-control-plane-03-SUMMARY.md`
</output>
