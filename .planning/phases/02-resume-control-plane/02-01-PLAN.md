---
phase: 02-resume-control-plane
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/model/run-record.ts
  - src/core/state/run-state-machine.ts
  - src/core/state/recovery-classifier.ts
  - tests/state-engines.test.ts
autonomous: true
requirements:
  - RECV-02
  - RECV-03
must_haves:
  truths:
    - "Recovery truth is derived from explicit classifications instead of transport-only resume flags."
    - "Persisted run phase and pendingAction describe the last durable boundary reached, not in-flight work."
    - "Ambiguous accepted-path partials are marked repair_required instead of guessed-resumable."
  artifacts:
    - path: src/core/state/recovery-classifier.ts
      provides: "Shared recovery classification contract and decision logic."
    - path: src/core/state/run-state-machine.ts
      provides: "Checkpoint-aligned phase and pendingAction transitions."
    - path: src/core/model/run-record.ts
      provides: "Persisted run schema for truthful checkpoint boundaries."
    - path: tests/state-engines.test.ts
      provides: "Regression coverage for recovery classification and durable phase semantics."
  key_links:
    - "RunRecord phase/pendingAction feed classifyRecovery without transport-specific branching."
    - "repair_required is emitted when decision/commit/frontier evidence is contradictory or missing."
    - "manual_review_blocked is separate from resumable automation for needs_human runs."
---

<objective>
Define the shared recovery contract Phase 2 builds on: truthful checkpoint-aligned run phases plus one recovery classifier that can distinguish idle, resumable, manual-review-blocked, and repair-required runs.

Purpose: Establish a stable state/policy layer before service, CLI, and lock behavior build on it.
Output: A shared recovery classifier, updated run-record/state-machine contracts, and regression coverage proving ambiguous partials are never auto-resumed.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-resume-control-plane/02-CONTEXT.md
@.planning/phases/02-resume-control-plane/02-RESEARCH.md
@.planning/phases/02-resume-control-plane/02-VALIDATION.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@docs/knowledge/decision-2026-03-29-four-defense-lines.md
@src/core/model/run-record.ts
@src/core/state/run-state-machine.ts
@tests/state-engines.test.ts

<interfaces>
From src/core/state/run-state-machine.ts:
```typescript
export interface RecoveryPlan {
  resumable: boolean;
  nextAction: PendingAction;
  reason: string;
}

export function canResume(run: RunRecord): boolean;
export function recoverRun(run: RunRecord): RecoveryPlan;
```

From src/core/model/run-record.ts:
```typescript
export const runPhaseSchema = z.enum([
  "proposed",
  "executed",
  "evaluated",
  "decision_written",
  "committed",
  "frontier_updated",
  "completed",
  "failed",
]);

export const pendingActionSchema = z.enum([
  "none",
  "execute_experiment",
  "evaluate_metrics",
  "write_decision",
  "commit_candidate",
  "update_frontier",
  "cleanup_workspace",
]);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared recovery classification contract</name>
  <files>src/core/model/run-record.ts
src/core/state/run-state-machine.ts
src/core/state/recovery-classifier.ts
tests/state-engines.test.ts</files>
  <read_first>.planning/phases/02-resume-control-plane/02-CONTEXT.md
.planning/phases/02-resume-control-plane/02-RESEARCH.md
src/core/model/run-record.ts
src/core/state/run-state-machine.ts
tests/state-engines.test.ts</read_first>
  <behavior>
    - Test 1: A latest run with no durable work returns classification `resumable` only when its saved phase and pendingAction describe a replay-safe next step.
    - Test 2: A `needs_human` run is classified as `manual_review_blocked` with `resumeAllowed=false`.
    - Test 3: A run with contradictory accepted-path evidence is classified as `repair_required` rather than resumable.
    - Test 4: Absence of a latest run maps to `idle`.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'idle|resumable|manual_review_blocked|repair_required' src/core/state/recovery-classifier.ts tests/state-engines.test.ts` returns matches for all four classifications.
    - `rg -n 'classifyRecovery|resumeAllowed|nextAction|reason' src/core/state/recovery-classifier.ts` returns matches.
    - `rg -n 'repair_required' tests/state-engines.test.ts` returns at least one assertion proving ambiguous partials are not resumed.
  </acceptance_criteria>
  <action>Create `src/core/state/recovery-classifier.ts` and move the recovery truth there per D-05, D-06, D-07, and D-08. The classifier must return `classification`, `nextAction`, `reason`, and `resumeAllowed`, and it must explicitly distinguish `idle`, `resumable`, `manual_review_blocked`, and `repair_required`. Extend `src/core/model/run-record.ts` and `src/core/state/run-state-machine.ts` only as needed so the classifier can express truthful checkpoint state without guessing. Do not introduce `--run-id` targeting, backward historical scans, repair tooling, or wait/retry lock modes.</action>
  <verify>
    <automated>npx vitest run tests/state-engines.test.ts</automated>
  </verify>
  <done>State-layer recovery classification exists, is schema-backed, and automated tests prove the four classifications and resumeAllowed semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make persisted run phases mean last durable checkpoint reached</name>
  <files>src/core/model/run-record.ts
src/core/state/run-state-machine.ts
tests/state-engines.test.ts</files>
  <read_first>src/core/model/run-record.ts
src/core/state/run-state-machine.ts
src/core/engine/cycle-runner.ts
.planning/phases/02-resume-control-plane/02-RESEARCH.md
tests/state-engines.test.ts</read_first>
  <behavior>
    - Test 1: The initial run state no longer claims `proposed` before proposal work has durably completed.
    - Test 2: Pending actions advance in checkpoint order without skipping from in-flight work to later steps.
    - Test 3: Accepted-path phases classify as `repair_required` when the run claims a later checkpoint without the durable evidence that checkpoint requires.
  </behavior>
  <acceptance_criteria>
    - `rg -n 'phase:' src/core/model/run-record.ts src/core/state/run-state-machine.ts` shows the updated checkpoint vocabulary used by the state machine.
    - `rg -n 'decision_written runs must include a decisionId|completed runs must include endedAt' src/core/model/run-record.ts` still returns the existing invariant checks after the checkpoint update.
    - `rg -n 'pendingAction' tests/state-engines.test.ts` returns assertions that the next action matches the durable checkpoint order.
  </acceptance_criteria>
  <action>Adjust the run phase and pending-action model so each persisted phase means "last durable boundary completed" per RECV-02 and the research recommendation to stop using `proposed` as an in-flight placeholder. Keep the brownfield CLI/MCP mental model intact, but rename or add phases inside the schema/state layer if needed so later plans can resume proposal, experiment, evaluation, decision writing, commit, and frontier update from truthful checkpoints. The accepted-path ambiguity boundary must stay narrow per D-08: missing durable evidence becomes `repair_required`, not a best-effort replay.</action>
  <verify>
    <automated>npx vitest run tests/state-engines.test.ts</automated>
  </verify>
  <done>The run schema and state machine encode durable checkpoints truthfully, and the state-engine suite proves that later plans can rely on phase/pendingAction without transport-specific overrides.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| persisted run JSON -> recovery classifier | untrusted partial state is interpreted into operator-visible recovery truth |
| cycle checkpoint model -> future resume dispatcher | incorrect checkpoint semantics would cause replay from the wrong step |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | T | `src/core/state/recovery-classifier.ts` | mitigate | Require explicit classification logic and emit `repair_required` when persisted evidence is contradictory or incomplete. |
| T-02-02 | R | `src/core/state/run-state-machine.ts` | mitigate | Make phase/pendingAction transitions checkpoint-aligned so later services can justify why a resume target is safe. |
| T-02-03 | D | `src/core/model/run-record.ts` | accept | File-per-record persistence remains local and simple in Phase 2; the mitigation is truthful classification rather than storage redesign. |
</threat_model>

<verification>
Run `npx vitest run tests/state-engines.test.ts` after each task. The phase foundation is only valid if the classifier and state machine agree on checkpoint order and ambiguity handling.
</verification>

<success_criteria>
- The codebase has one shared recovery classification contract covering `idle`, `resumable`, `manual_review_blocked`, and `repair_required`.
- Persisted checkpoint semantics no longer imply a completed proposal before proposal work actually exists.
- State-engine tests fail if accepted-path ambiguity or needs-human runs are treated as resumable automation.
</success_criteria>

<output>
After completion, create `.planning/phases/02-resume-control-plane/02-resume-control-plane-01-SUMMARY.md`
</output>
