---
phase: 03-promotion-durability-frontier-persistence
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/core/state/frontier-materializer.ts
  - src/app/services/run-cycle-service.ts
  - src/app/services/project-state-service.ts
  - src/adapters/fs/json-file-frontier-store.ts
  - tests/project-state-service.test.ts
  - tests/run-cycle-service.test.ts
  - tests/cli-commands.test.ts
autonomous: true
requirements:
  - STAB-02
must_haves:
  truths:
    - "Frontier state is rebuildable from durable run and decision records."
    - "`frontier.json` is a materialized snapshot, not the only source of truth."
    - "Status, inspect, frontier, and run execution self-heal when the snapshot is missing, corrupt, or stale."
  artifacts:
    - path: src/core/state/frontier-materializer.ts
      provides: "Authoritative frontier rebuild and snapshot reconciliation logic."
    - path: src/app/services/run-cycle-service.ts
      provides: "Run-time use of rebuilt frontier state before new cycle execution."
    - path: src/app/services/project-state-service.ts
      provides: "Read-model self-healing for status, inspect, and frontier surfaces."
    - path: src/adapters/fs/json-file-frontier-store.ts
      provides: "Safer snapshot persistence semantics."
    - path: tests/project-state-service.test.ts
      provides: "Rebuild and self-heal regression coverage for read models."
    - path: tests/cli-commands.test.ts
      provides: "CLI proof that frontier output still works after snapshot loss."
  key_links:
    - "Rebuild replays accepted decisions through the same frontier engine used by live execution."
    - "Services return rebuilt frontier state and rewrite the snapshot when it drifts."
---

<objective>
Make frontier persistence rebuildable and self-healing by materializing it from durable run and decision records instead of trusting `frontier.json` blindly.

Purpose: Ensure accepted-path state stays internally consistent after crashes or partial snapshot failures.
Output: A frontier materializer/rebuilder, service adoption of that materialized view, and regression coverage for missing or stale snapshot recovery.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-promotion-durability-frontier-persistence/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/core/state/frontier-engine.ts
@src/app/services/run-cycle-service.ts
@src/app/services/project-state-service.ts
@src/adapters/fs/json-file-frontier-store.ts
@tests/project-state-service.test.ts
@tests/run-cycle-service.test.ts
@tests/cli-commands.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing coverage for frontier rebuild and snapshot self-healing</name>
  <files>tests/project-state-service.test.ts
tests/run-cycle-service.test.ts
tests/cli-commands.test.ts</files>
  <read_first>.planning/phases/03-promotion-durability-frontier-persistence/03-CONTEXT.md
tests/project-state-service.test.ts
tests/run-cycle-service.test.ts
tests/cli-commands.test.ts
src/app/services/project-state-service.ts
src/app/services/run-cycle-service.ts</read_first>
  <behavior>
    - Test 1: Status/frontier surfaces rebuild frontier state when `frontier.json` is missing.
    - Test 2: Services overwrite a stale or contradictory snapshot with rebuilt frontier state.
    - Test 3: A new run uses rebuilt frontier state instead of a missing snapshot when evaluating against an incumbent.
  </behavior>
  <action>Write failing integration coverage that proves the runtime can recover frontier state from durable records without relying on a preexisting valid snapshot file. Keep rebuild semantics anchored to the current single-best frontier behavior already used in production code.</action>
  <verify>
    <automated>npx vitest run tests/project-state-service.test.ts tests/run-cycle-service.test.ts tests/cli-commands.test.ts</automated>
  </verify>
  <done>There is failing coverage for missing/stale frontier snapshots before materialization logic is introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Materialize frontier state from durable records and self-heal snapshots</name>
  <files>src/core/state/frontier-materializer.ts
src/app/services/run-cycle-service.ts
src/app/services/project-state-service.ts
src/adapters/fs/json-file-frontier-store.ts
tests/project-state-service.test.ts
tests/run-cycle-service.test.ts
tests/cli-commands.test.ts</files>
  <read_first>src/core/state/frontier-engine.ts
src/app/services/run-cycle-service.ts
src/app/services/project-state-service.ts
src/adapters/fs/json-file-frontier-store.ts
tests/project-state-service.test.ts
tests/run-cycle-service.test.ts
tests/cli-commands.test.ts</read_first>
  <behavior>
    - Test 1: Rebuild iterates accepted decisions in order and reproduces the authoritative frontier.
    - Test 2: Missing, stale, or invalid snapshots are replaced with rebuilt frontier state.
    - Test 3: Run execution, status, inspect, and frontier all consume the same materialized frontier view.
  </behavior>
  <action>Create one frontier materializer that rebuilds from accepted run/decision records using the existing frontier engine, adopt it in run and project-state services, and make snapshot writes safer. Keep `frontier.json` as a cache/materialization, not a second policy source. Do not add a new CLI repair command in this plan.</action>
  <verify>
    <automated>npx vitest run tests/project-state-service.test.ts tests/run-cycle-service.test.ts tests/cli-commands.test.ts</automated>
  </verify>
  <done>Frontier state can be rebuilt and self-healed from durable records, and all operator-facing services read the same authoritative materialized view.</done>
</task>

</tasks>

<success_criteria>
- `frontier.json` can disappear or drift without breaking run execution or read models.
- Frontier rebuild uses the same update logic as live accepted runs.
- Services automatically restore a consistent frontier snapshot from durable records.
</success_criteria>

<output>
After completion, create `.planning/phases/03-promotion-durability-frontier-persistence/03-promotion-durability-frontier-persistence-02-SUMMARY.md`
</output>
