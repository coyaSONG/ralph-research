---
phase: 03-promotion-durability-frontier-persistence
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/engine/workspace-manager.ts
  - src/adapters/git/git-client.ts
  - src/core/engine/cycle-runner.ts
  - src/core/state/recovery-classifier.ts
  - src/core/model/run-record.ts
  - tests/run-cycle-service.test.ts
  - tests/state-engines.test.ts
autonomous: true
requirements:
  - STAB-01
must_haves:
  truths:
    - "Accepted runs persist a replayable promotion bundle before promotion mutates the repo."
    - "Automated promotion replays from a durable patch instead of copying files directly out of the workspace."
    - "Resume after an interrupted promotion can detect an already-applied patch and continue on the same run."
  artifacts:
    - path: src/core/engine/workspace-manager.ts
      provides: "Promotion bundle generation from workspace state without mutating the repo root."
    - path: src/adapters/git/git-client.ts
      provides: "Patch replay helper that distinguishes apply-needed from already-applied."
    - path: src/core/engine/cycle-runner.ts
      provides: "Accepted-path orchestration that persists and reuses a durable promotion patch."
    - path: tests/run-cycle-service.test.ts
      provides: "End-to-end regression coverage for accepted-path durability and replay."
    - path: tests/state-engines.test.ts
      provides: "Recovery evidence assertions for accepted checkpoints that now require a durable patch."
  key_links:
    - "decision_written accepted checkpoints must carry a durable patch path before commit replay is considered safe."
    - "commit replay uses the persisted patch and bounded changed paths, not ad hoc workspace copying."
---

<objective>
Persist a durable promotion bundle for accepted runs and replace file-by-file workspace promotion with patch-driven replay that is safe to resume on the same `runId`.

Purpose: Remove the most ambiguous accepted-path mutation sequence and make post-decision recovery depend on durable artifacts rather than the transient workspace state alone.
Output: A persisted promotion patch, patch-aware commit replay, tighter accepted-path recovery evidence, and regression coverage.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-promotion-durability-frontier-persistence/03-CONTEXT.md
@.planning/codebase/CONCERNS.md
@src/core/engine/cycle-runner.ts
@src/core/engine/workspace-manager.ts
@src/adapters/git/git-client.ts
@src/core/state/recovery-classifier.ts
@src/core/model/run-record.ts
@tests/run-cycle-service.test.ts
@tests/state-engines.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add accepted-path durability tests before changing promotion replay</name>
  <files>tests/run-cycle-service.test.ts
tests/state-engines.test.ts</files>
  <read_first>.planning/phases/03-promotion-durability-frontier-persistence/03-CONTEXT.md
tests/run-cycle-service.test.ts
tests/state-engines.test.ts
src/core/engine/cycle-runner.ts
src/core/engine/workspace-manager.ts
src/adapters/git/git-client.ts</read_first>
  <behavior>
    - Test 1: An accepted run persists a durable promotion patch path before completion.
    - Test 2: Resuming an accepted run after the durable patch is already applied still commits on the same `runId`.
    - Test 3: An accepted `decision_written` checkpoint without a durable patch is no longer treated as safely resumable.
  </behavior>
  <action>Extend the accepted-path integration and state-engine tests so they fail unless accepted checkpoints carry a persisted patch and commit replay tolerates an already-applied patch. Keep the scope on automated acceptance only; do not change manual-review tests in this plan.</action>
  <verify>
    <automated>npx vitest run tests/run-cycle-service.test.ts tests/state-engines.test.ts</automated>
  </verify>
  <done>There is failing regression coverage for durable promotion artifacts and already-applied patch replay before the implementation changes land.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace automated workspace promotion with durable patch replay</name>
  <files>src/core/engine/workspace-manager.ts
src/adapters/git/git-client.ts
src/core/engine/cycle-runner.ts
src/core/state/recovery-classifier.ts
src/core/model/run-record.ts
tests/run-cycle-service.test.ts
tests/state-engines.test.ts</files>
  <read_first>src/core/engine/workspace-manager.ts
src/adapters/git/git-client.ts
src/core/engine/cycle-runner.ts
src/core/state/recovery-classifier.ts
src/core/model/run-record.ts
tests/run-cycle-service.test.ts
tests/state-engines.test.ts</read_first>
  <behavior>
    - Test 1: Accepted runs persist a promotion patch and changed-path metadata before advancing to `decision_written`.
    - Test 2: Commit replay applies the patch if needed, or detects an already-applied patch and continues safely.
    - Test 3: Recovery truth rejects accepted checkpoints that claim resume safety without the durable promotion bundle.
  </behavior>
  <action>Implement durable promotion preparation in the workspace manager, replay it through the Git client, and update the cycle runner to use that path for automated acceptance. Tighten recovery evidence so accepted checkpoints without the durable patch are classified `repair_required`. Do not reintroduce the old direct-copy promotion flow for automated runs.</action>
  <verify>
    <automated>npx vitest run tests/run-cycle-service.test.ts tests/state-engines.test.ts</automated>
  </verify>
  <done>The accepted path is patch-driven, durable artifacts exist before repo mutation, and same-run replay can finish after an interrupted apply/commit boundary.</done>
</task>

</tasks>

<success_criteria>
- Accepted checkpoints carry a durable promotion artifact before commit replay starts.
- Automated promotion no longer depends on direct file copying out of the workspace.
- Resume after an interrupted accepted path can continue from durable records even if the patch is already present in the repo.
</success_criteria>

<output>
After completion, create `.planning/phases/03-promotion-durability-frontier-persistence/03-promotion-durability-frontier-persistence-01-SUMMARY.md`
</output>
