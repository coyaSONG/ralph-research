---
phase: 01-contract-truth-run-admission
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - src/app/services/run-admission-service.ts
  - src/cli/commands/doctor.ts
  - src/cli/commands/validate.ts
  - src/cli/main.ts
  - tests/validate-command.test.ts
  - tests/cli-commands.test.ts
autonomous: true
requirements:
  - CONT-03
must_haves:
  truths:
    - `validate` and `doctor` report the same manifest admission truth as the run path, including unsupported baseline declarations.
    - Operators can distinguish executable manifests from capability-mismatched manifests without starting a run.
    - CLI preflight diagnostics stay on the existing surface instead of introducing a new command family.
  artifacts:
    - path: src/app/services/run-admission-service.ts
      provides: Shared preflight service for CLI-facing admission checks.
    - path: src/cli/commands/doctor.ts
      provides: Real doctor command output based on manifest admission.
    - path: tests/validate-command.test.ts
      provides: Regression coverage for validate preflight output.
    - path: tests/cli-commands.test.ts
      provides: Regression coverage for doctor and run command consistency.
  key_links:
    - from: src/cli/commands/validate.ts
      to: src/app/services/run-admission-service.ts
      via: Shared preflight result formatting
      pattern: runAdmission|getRunAdmission
    - from: src/cli/commands/doctor.ts
      to: src/app/services/run-admission-service.ts
      via: Same capability service as validate and run
      pattern: runAdmission|getRunAdmission
---

<objective>
Unify the operator-facing preflight surface so `validate`, `doctor`, and the run path report the same admission truth before a cycle begins.

Purpose: Make capability mismatches observable through stable CLI preflight commands instead of being discoverable only during execution.
Output: A shared admission service, a real `doctor` command, and CLI regression tests for consistent preflight diagnostics including baseline admission failures.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
@src/cli/main.ts
@src/cli/commands/validate.ts
@src/app/context.ts
@tests/validate-command.test.ts
@tests/cli-commands.test.ts

<interfaces>
From src/cli/commands/validate.ts:
```typescript
export interface ValidateCommandOptions {
  path: string;
  json?: boolean;
}

export async function runValidateCommand(
  options: ValidateCommandOptions,
  io?: ValidateCommandIO,
): Promise<number>;
```

From src/cli/main.ts:
```typescript
program
  .command("doctor")
  .description("Print scaffold status.")
  .action(() => {
    const context = createAppContext();
    logger.info({ appName: context.appName, phase: context.phase }, "scaffold ready");
  });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace scaffold-only doctor with shared admission preflight</name>
  <read_first>
.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
src/cli/main.ts
src/cli/commands/validate.ts
src/app/context.ts
src/core/manifest/admission.ts
src/adapters/fs/manifest-loader.ts
  </read_first>
  <files>src/app/services/run-admission-service.ts, src/cli/commands/doctor.ts, src/cli/commands/validate.ts, src/cli/main.ts</files>
  <behavior>
    - Test 1: `doctor` exits success for executable manifests and non-zero for capability-mismatched or unresolved-`baselineRef` manifests, using the same admission rules as `validate` and `run`.
    - Test 2: `validate --json` still emits machine-readable output, but now includes truthful admission failures instead of schema-only success.
  </behavior>
  <action>Create `src/app/services/run-admission-service.ts` as a thin service that loads the manifest and returns an executable-or-blocked admission result without starting a cycle. This service must reuse the repo-aware admission behavior introduced in Plan 02 so CLI preflight can surface unsupported `baselineRef` declarations with the same truth as `run`. Add `src/cli/commands/doctor.ts` and register it from `src/cli/main.ts`, keeping the command name `doctor` and not adding a new command family per D-06. Update `runValidateCommand()` to call the same service instead of performing its own loader-only flow. Keep output concrete: success should state the manifest is executable, and failures should print the explicit unsupported capability issues gathered from the shared admission path.</action>
  <acceptance_criteria>
`test -f src/app/services/run-admission-service.ts`
`test -f src/cli/commands/doctor.ts`
`rg -n 'doctor' src/cli/main.ts src/cli/commands/doctor.ts`
`rg -n 'runAdmission|getRunAdmission' src/cli/commands/validate.ts src/cli/commands/doctor.ts src/app/services/run-admission-service.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/validate-command.test.ts tests/cli-commands.test.ts</automated>
  </verify>
  <done>`doctor` is a real admission preflight command and `validate` reuses the same service instead of duplicating manifest logic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock the CLI preflight surface with regression tests</name>
  <read_first>
tests/validate-command.test.ts
tests/cli-commands.test.ts
src/cli/commands/doctor.ts
src/cli/commands/validate.ts
src/cli/commands/run.ts
  </read_first>
  <files>tests/validate-command.test.ts, tests/cli-commands.test.ts</files>
  <behavior>
    - Test 1: `validate` rejects unsupported workspace, proposer, and baseline fixtures with stable error payloads.
    - Test 2: `doctor` reports executable vs blocked manifests using the same truth as `run`, and `run` still aborts unsupported manifests or unresolved baseline refs with non-zero exit codes.
  </behavior>
  <action>Extend the existing command tests instead of creating a parallel harness. In `tests/validate-command.test.ts`, assert both plain-text and JSON capability failures for the unsupported fixtures introduced in Plan 01 plus a repo-backed unresolved-`baselineRef` case using the run-path admission behavior from Plan 02. In `tests/cli-commands.test.ts`, add a `doctor` command case for an executable repo and blocked repos, plus `runRunCommand()` cases that prove unsupported manifests and unresolved baseline refs stop with a non-zero exit code and no side effects. Reuse the current capturing IO helpers so the tests stay aligned with existing CLI patterns.</action>
  <acceptance_criteria>
`rg -n 'doctor|unsupported workspace|operator_llm|baselineRef' tests/validate-command.test.ts tests/cli-commands.test.ts`
`npx vitest run tests/validate-command.test.ts tests/cli-commands.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/validate-command.test.ts tests/cli-commands.test.ts</automated>
  </verify>
  <done>The CLI has regression coverage proving `validate`, `doctor`, and `run` expose the same manifest admission truth for unsupported workspace, proposer, and baseline declarations before a destructive cycle starts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator CLI invocation -> preflight service | User-controlled manifest paths and repo roots are evaluated before execution. |
| preflight result -> CLI output | Admission truth must remain consistent across commands to avoid misleading operators. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07 | R | `src/cli/commands/doctor.ts` + `src/cli/commands/validate.ts` | mitigate | Route both commands through the same service so they cannot disagree about executable vs blocked manifests. |
| T-01-08 | I | CLI output formatting | mitigate | Emit explicit unsupported-capability details instead of generic scaffold logs or silent success. |
| T-01-09 | T | `tests/cli-commands.test.ts` | mitigate | Add command-level regression coverage that fails if doctor/validate/run drift apart again. |
</threat_model>

<verification>
Run `npx vitest run tests/validate-command.test.ts tests/cli-commands.test.ts`. If doctor output format changes, also run `npm run typecheck` before closing the plan because the new command registration touches `src/cli/main.ts`.
</verification>

<success_criteria>
Operators can use `validate` or `doctor` to see the same executable-or-blocked admission truth that the run path enforces, including unsupported baseline declarations, without starting a cycle or learning a new surface.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-truth-run-admission/01-contract-truth-run-admission-03-SUMMARY.md`
</output>
