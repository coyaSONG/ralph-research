---
phase: 01-contract-truth-run-admission
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/app/services/run-cycle-service.ts
  - src/core/engine/workspace-manager.ts
  - src/core/engine/cycle-runner.ts
  - tests/run-cycle-service.test.ts
  - tests/lockfile-workspace-manager.test.ts
autonomous: true
requirements:
  - CONT-02
must_haves:
  truths:
    - Supported `project.baselineRef` values affect the actual workspace starting point instead of being ignored.
    - `run` cannot create a workspace or persisted run record when admission says the manifest is unsupported.
    - Admission happens before repo mutation, not after a partial candidate run has already started.
  artifacts:
    - path: src/app/services/run-cycle-service.ts
      provides: Shared run-path admission gate before destructive execution.
    - path: src/core/engine/workspace-manager.ts
      provides: Baseline-aware git workspace creation for supported manifests.
    - path: tests/run-cycle-service.test.ts
      provides: Regression coverage for fail-fast run admission and baseline behavior.
  key_links:
    - from: src/app/services/run-cycle-service.ts
      to: src/core/manifest/admission.ts
      via: Shared preflight before execution
      pattern: admission|capability
    - from: src/core/engine/workspace-manager.ts
      to: src/core/engine/cycle-runner.ts
      via: baselineRef-aware workspace creation
      pattern: createWorkspace|baselineRef
---

<objective>
Make the execution path honor supported manifest behavior and reject unsupported manifests before any run mutates repo state.

Purpose: Ensure `run` only starts executable manifests and that supported baseline configuration changes runtime behavior exactly as promised.
Output: A preflighted run service, baseline-aware workspace creation, and integration tests that prove no destructive side effects happen on unsupported manifests.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
@src/app/services/run-cycle-service.ts
@src/core/engine/workspace-manager.ts
@src/core/engine/cycle-runner.ts
@tests/run-cycle-service.test.ts
@tests/lockfile-workspace-manager.test.ts

<interfaces>
From src/app/services/run-cycle-service.ts:
```typescript
export interface RunCycleServiceInput {
  repoRoot: string;
  manifestPath?: string;
  resume?: boolean;
}

export class RunCycleService {
  public async run(input: RunCycleServiceInput): Promise<RunCycleServiceResult>;
}
```

From src/core/engine/workspace-manager.ts:
```typescript
export class GitWorktreeWorkspaceManager {
  public async createWorkspace(candidateId: string): Promise<WorkspaceInfo>;
  public async promoteWorkspace(candidateId: string, options?: PromoteWorkspaceOptions): Promise<PromoteWorkspaceResult>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gate the run path before lock, workspace, and run-record mutation</name>
  <read_first>
.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
src/core/manifest/admission.ts
src/app/services/run-cycle-service.ts
src/core/engine/workspace-manager.ts
src/core/engine/cycle-runner.ts
  </read_first>
  <files>src/app/services/run-cycle-service.ts, src/core/engine/workspace-manager.ts, src/core/engine/cycle-runner.ts</files>
  <behavior>
    - Test 1: Unsupported manifests fail `RunCycleService.run()` before `.ralph/runs`, `.ralph/workspaces`, or Git worktree mutation occurs per D-03 and D-05.
    - Test 2: Supported git manifests use `project.baselineRef` as the worktree checkout target instead of always using `HEAD`, satisfying CONT-02.
  </behavior>
  <action>Update `RunCycleService.run()` so the shared admission check executes before lock acquisition and before store/workspace setup. Keep the existing CLI/MCP return shape by surfacing admission failure as the same top-level command/service error path rather than inventing a new transport. Extend `GitWorktreeWorkspaceManager.createWorkspace()` to accept the admitted baseline ref and call `git worktree add --detach <workspacePath> <baselineRef>` instead of hard-coding `HEAD`. Thread the admitted baseline through `cycle-runner.ts` without changing the CLI or MCP command names.</action>
  <acceptance_criteria>
`rg -n 'worktree add --detach.*baselineRef|createWorkspace\\(' src/core/engine/workspace-manager.ts src/core/engine/cycle-runner.ts`
`rg -n 'admission|capability' src/app/services/run-cycle-service.ts`
`npx vitest run tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts</automated>
  </verify>
  <done>`RunCycleService` rejects unsupported manifests before destructive work begins, and supported baseline refs are no longer ignored by git workspace creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove fail-fast admission and baseline behavior with integration coverage</name>
  <read_first>
tests/run-cycle-service.test.ts
tests/lockfile-workspace-manager.test.ts
src/app/services/run-cycle-service.ts
src/core/engine/workspace-manager.ts
.planning/codebase/CONCERNS.md
  </read_first>
  <files>tests/run-cycle-service.test.ts, tests/lockfile-workspace-manager.test.ts</files>
  <behavior>
    - Test 1: A manifest with `workspace: copy` or `operator_llm` causes `RunCycleService.run()` to fail without creating run records, decisions, or workspaces.
    - Test 2: A repo with a non-HEAD baseline ref proves the workspace starts from that ref, not from the currently checked-out commit.
  </behavior>
  <action>Extend the integration fixtures so they cover one unsupported workspace case, one unsupported proposer case, and one supported non-HEAD baseline case. Assert concrete side effects: no `.ralph/runs/run-*` file, no `.ralph/workspaces/candidate-*` directory, and no extra Git commit on rejected admission paths; assert supported baseline runs read the file contents from the requested ref. Keep the tests in the existing temp-repo style used across the suite.</action>
  <acceptance_criteria>
`rg -n 'workspace: copy|operator_llm|baselineRef' tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts`
`npx vitest run tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts</automated>
  </verify>
  <done>Integration tests prove unsupported manifests stop before mutation and supported baseline refs measurably change runtime workspace behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/MCP run request -> `RunCycleService` | Operator-controlled manifest path and repo root enter the destructive execution path. |
| admitted manifest -> git worktree creation | Baseline refs and workspace parameters cross into Git side effects. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | T | `src/app/services/run-cycle-service.ts` | mitigate | Run admission before lock/workspace/run-store mutation so unsupported manifests cannot tamper with repo state. |
| T-01-05 | E | `src/core/engine/workspace-manager.ts` | mitigate | Pass only the admitted baseline ref into `git worktree add` and keep unsupported workspace modes rejected upstream. |
| T-01-06 | R | `tests/run-cycle-service.test.ts` | mitigate | Add side-effect assertions proving rejected admission leaves no run files, workspaces, or commits behind. |
</threat_model>

<verification>
Run `npx vitest run tests/run-cycle-service.test.ts tests/lockfile-workspace-manager.test.ts`. For manual spot checks after tests pass, create a temp repo with a tagged baseline and confirm the workspace file contents match the requested ref.
</verification>

<success_criteria>
`run` refuses unsupported manifests before repo mutation starts, and supported git manifests demonstrably honor `project.baselineRef` instead of always using `HEAD`.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-truth-run-admission/01-contract-truth-run-admission-02-SUMMARY.md`
</output>
