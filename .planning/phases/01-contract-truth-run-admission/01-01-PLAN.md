---
phase: 01-contract-truth-run-admission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/manifest/admission.ts
  - src/core/manifest/schema.ts
  - src/adapters/fs/manifest-loader.ts
  - tests/manifest-loader.test.ts
  - tests/fixtures/manifests/invalid-unsupported-workspace.ralph.yaml
  - tests/fixtures/manifests/invalid-unsupported-operator-llm.ralph.yaml
autonomous: true
requirements:
  - CONT-01
must_haves:
  truths:
    - Unsupported proposer and workspace declarations are rejected with explicit admission errors instead of passing schema validation.
    - A manifest can be classified as executable or capability-mismatched before any run starts.
    - Admission rules are defined once in code instead of being reimplemented ad hoc per command.
  artifacts:
    - path: src/core/manifest/admission.ts
      provides: Shared manifest capability and admission rules.
    - path: src/adapters/fs/manifest-loader.ts
      provides: Two-stage manifest loading that applies schema parsing and admission checks together.
    - path: tests/manifest-loader.test.ts
      provides: Regression coverage for unsupported surface rejection.
  key_links:
    - from: src/adapters/fs/manifest-loader.ts
      to: src/core/manifest/admission.ts
      via: Shared post-schema admission compilation
      pattern: compile.*admission|assess.*capability
    - from: tests/manifest-loader.test.ts
      to: src/adapters/fs/manifest-loader.ts
      via: Invalid manifest fixtures
      pattern: invalid-unsupported-(workspace|operator-llm)
---

<objective>
Define the truthful manifest admission contract for Phase 1 so unsupported surface is rejected explicitly instead of leaking through schema-only validation.

Purpose: Make the control plane honest before execution paths reuse it.
Output: A shared admission module, loader integration, and regression fixtures for unsupported proposer/workspace cases.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
@.planning/research/SUMMARY.md
@.planning/codebase/CONCERNS.md
@src/core/manifest/schema.ts
@src/adapters/fs/manifest-loader.ts
@tests/manifest-loader.test.ts

<interfaces>
From src/core/manifest/schema.ts:
```typescript
export const projectSchema = z.object({
  name: z.string().min(1),
  artifact: z.enum(["code", "manuscript", "literature_review", "prompt", "generic"]),
  baselineRef: z.string().min(1).default(DEFAULT_PROJECT_BASELINE_REF),
  workspace: z.enum(["git", "copy"]).default(DEFAULT_PROJECT_WORKSPACE),
});

export const proposerSchema = z.discriminatedUnion("type", [
  commandProposerSchema,
  operatorLlmProposerSchema,
  parallelProposerSchema,
]);
```

From src/adapters/fs/manifest-loader.ts:
```typescript
export interface LoadedManifest {
  path: string;
  manifest: RalphManifest;
}

export async function loadManifestFromFile(path = DEFAULT_MANIFEST_FILENAME): Promise<LoadedManifest>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the shared admission contract and rejection matrix</name>
  <read_first>
.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
.planning/codebase/CONCERNS.md
src/core/manifest/schema.ts
src/adapters/fs/manifest-loader.ts
tests/manifest-loader.test.ts
  </read_first>
  <files>src/core/manifest/admission.ts, src/core/manifest/schema.ts, tests/manifest-loader.test.ts</files>
  <behavior>
    - Test 1: `project.workspace: copy` is rejected with a path-specific unsupported-capability error per D-01 and D-03.
    - Test 2: `proposer.type: operator_llm` is rejected with a path-specific unsupported-capability error per D-01 and D-02.
    - Test 3: `proposer.type: parallel` remains admissible only when every strategy is `command`, preserving current truthful support instead of shrinking working command flows.
  </behavior>
  <action>Create `src/core/manifest/admission.ts` as the single capability source of truth. Export an admission result type and a compiler/check function that operates on a parsed `RalphManifest` plus repo-relative context. Encode the concrete Phase 1 policy: allow `project.workspace="git"`, reject `project.workspace="copy"`, reject top-level `operator_llm`, reject any parallel strategy whose leaf proposer is not `command`, and keep the rule messages explicit about what is unsupported and why. Keep schema breadth only where it improves diagnostics; do not add fake support or silent fallback.</action>
  <acceptance_criteria>
`test -f src/core/manifest/admission.ts`
`rg -n 'workspace.*copy|operator_llm|parallel' src/core/manifest/admission.ts`
`npx vitest run tests/manifest-loader.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/manifest-loader.test.ts</automated>
  </verify>
  <done>Unsupported workspace and proposer declarations are represented by shared admission rules and failing tests, with no command-specific logic branches required to discover them.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply admission checks during manifest load and add rejection fixtures</name>
  <read_first>
.planning/phases/01-contract-truth-run-admission/01-CONTEXT.md
src/core/manifest/admission.ts
src/adapters/fs/manifest-loader.ts
tests/fixtures/manifests/valid-writing.ralph.yaml
tests/manifest-loader.test.ts
  </read_first>
  <files>src/adapters/fs/manifest-loader.ts, tests/manifest-loader.test.ts, tests/fixtures/manifests/invalid-unsupported-workspace.ralph.yaml, tests/fixtures/manifests/invalid-unsupported-operator-llm.ralph.yaml</files>
  <behavior>
    - Test 1: Schema-valid but unsupported manifests fail `loadManifestFromFile()` with explicit admission details.
    - Test 2: Existing supported manifests still load successfully without losing defaults or changing their parsed shape.
  </behavior>
  <action>Update `loadManifestFromFile()` to remain a two-stage boundary: YAML parse plus Zod schema parse first, then shared admission compilation second. Preserve `ManifestLoadError`, but include the admission issue payload in `causeValue` so CLI surfaces can reuse it later. Add dedicated unsupported fixture manifests for `workspace: copy` and `operator_llm`, then extend `tests/manifest-loader.test.ts` to assert those files fail with actionable details while existing supported fixtures still pass unchanged.</action>
  <acceptance_criteria>
`test -f tests/fixtures/manifests/invalid-unsupported-workspace.ralph.yaml`
`test -f tests/fixtures/manifests/invalid-unsupported-operator-llm.ralph.yaml`
`rg -n 'unsupported workspace|operator_llm' tests/manifest-loader.test.ts`
`npx vitest run tests/manifest-loader.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/manifest-loader.test.ts</automated>
  </verify>
  <done>The manifest loader rejects unsupported surface through the shared admission module, and unsupported fixtures fail before any execution code is involved.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| manifest file -> manifest loader | Untrusted local configuration crosses into runtime compilation. |
| parsed manifest -> admission compiler | Schema-valid input still needs capability truth enforcement before execution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | T | `src/core/manifest/schema.ts` + `src/core/manifest/admission.ts` | mitigate | Centralize supported-surface rules so unsupported values cannot drift between commands. |
| T-01-02 | D | `src/adapters/fs/manifest-loader.ts` | mitigate | Fail fast during load with explicit issues instead of letting runtime paths create workspaces and fail later. |
| T-01-03 | R | `tests/manifest-loader.test.ts` | mitigate | Add regression fixtures that prove unsupported surface stays rejected and supported fixtures stay loadable. |
</threat_model>

<verification>
Run `npx vitest run tests/manifest-loader.test.ts` after both tasks. If loader payload shape changes, also re-run `npx vitest run tests/validate-command.test.ts` before handing off to the next plan.
</verification>

<success_criteria>
Schema-valid manifests that declare unsupported workspace or proposer surface fail at the manifest boundary with explicit details, and supported manifests still parse with the same defaults as before.
</success_criteria>

<output>
After completion, create `.planning/phases/01-contract-truth-run-admission/01-contract-truth-run-admission-01-SUMMARY.md`
</output>
