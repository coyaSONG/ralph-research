---
phase: 04-manual-review-semantic-unification
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/services/manual-decision-service.ts
  - src/core/engine/cycle-runner.ts
  - src/core/state/frontier-materializer.ts
  - src/core/state/frontier-semantics.ts
  - src/core/engine/promotion-artifact.ts
  - tests/cli-commands.test.ts
autonomous: true
requirements:
  - REVW-01
  - REVW-02
  - REVW-04
must_haves:
  truths:
    - "Manual approval uses the same durable promotion and frontier-update semantics as automated acceptance."
    - "Manual reject keeps frontier membership unchanged but records truthful before/after frontier IDs."
    - "Manual review advances runs through the existing checkpoint vocabulary instead of bypassing recovery semantics."
  artifacts:
    - path: src/app/services/manual-decision-service.ts
      provides: "Shared manual accept/reject orchestration built on durable promotion and frontier semantics."
    - path: src/core/state/frontier-semantics.ts
      provides: "One authoritative accepted-frontier entry and update path shared by automation, manual review, and rebuild."
    - path: src/core/engine/promotion-artifact.ts
      provides: "Durable promotion artifact preparation usable outside the cycle runner."
    - path: tests/cli-commands.test.ts
      provides: "Regression coverage for manual accept/reject semantics and frontier behavior."
  key_links:
    - "Manual review must stop overwriting frontier snapshots with service-local semantics."
    - "Human acceptance time, not the original `needs_human` timestamp, must drive durable frontier replay ordering."
---

<objective>
Unify manual accept and reject with the durable promotion, checkpoint progression, and frontier-update semantics already used by automated acceptance.

Purpose: Remove the last major place where a human-reviewed run can mean something different from an automatically accepted run.
Output: Shared frontier semantics, reusable promotion-artifact helpers, truthful manual decision records, and regression coverage for accept/reject behavior.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-manual-review-semantic-unification/04-CONTEXT.md
@src/app/services/manual-decision-service.ts
@src/core/engine/cycle-runner.ts
@src/core/state/frontier-materializer.ts
@src/core/state/run-state-machine.ts
@tests/cli-commands.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing manual-review semantic tests</name>
  <files>tests/cli-commands.test.ts</files>
  <read_first>.planning/phases/04-manual-review-semantic-unification/04-CONTEXT.md
tests/cli-commands.test.ts
src/app/services/manual-decision-service.ts</read_first>
  <behavior>
    - Test 1: Manual accept under a `pareto` frontier keeps non-dominated incumbents instead of collapsing to one entry.
    - Test 2: Manual reject keeps frontier membership unchanged and cleans up the workspace.
    - Test 3: Manual accept persists durable patch metadata before commit and records truthful frontier IDs.
  </behavior>
  <action>Extend the CLI-level regression coverage so the current divergent manual-review behavior fails before the implementation changes land. Favor real temp repos and persisted stores over mocks.</action>
  <verify>
    <automated>npx vitest run tests/cli-commands.test.ts</automated>
  </verify>
  <done>There is failing regression coverage for the current manual frontier overwrite and reject-path inconsistency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace divergent manual-review behavior with shared semantics</name>
  <files>src/app/services/manual-decision-service.ts
src/core/engine/cycle-runner.ts
src/core/state/frontier-materializer.ts
src/core/state/frontier-semantics.ts
src/core/engine/promotion-artifact.ts
tests/cli-commands.test.ts</files>
  <read_first>src/app/services/manual-decision-service.ts
src/core/engine/cycle-runner.ts
src/core/state/frontier-materializer.ts
src/core/state/run-state-machine.ts
tests/cli-commands.test.ts</read_first>
  <behavior>
    - Test 1: Manual accept uses a durable promotion patch and bounded changed paths before committing.
    - Test 2: Manual accept computes frontier change through the shared single-best/pareto update logic.
    - Test 3: Manual reject records truthful frontier IDs and advances the run through cleanup-consistent checkpoints.
  </behavior>
  <action>Refactor the shared frontier and promotion helpers as needed, then update `ManualDecisionService` to use them and to advance runs through truthful checkpoints. Keep CLI output stable unless the shared semantics require new persisted fields.</action>
  <verify>
    <automated>npx vitest run tests/cli-commands.test.ts</automated>
  </verify>
  <done>Manual accept/reject no longer have their own frontier semantics, and accepted manual decisions use the same durable promotion path as automation.</done>
</task>

</tasks>

<success_criteria>
- Manual accept preserves `single_best` and `pareto` frontier semantics instead of overwriting them.
- Manual reject leaves frontier membership unchanged while keeping run and decision state truthful.
- Manual approval now uses the same durable promotion semantics as automated acceptance.
</success_criteria>

<output>
After completion, create `.planning/phases/04-manual-review-semantic-unification/04-manual-review-semantic-unification-01-SUMMARY.md`
</output>
