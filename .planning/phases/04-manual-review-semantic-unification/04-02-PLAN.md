---
phase: 04-manual-review-semantic-unification
plan: "02"
type: execute
wave: 1
depends_on:
  - 04-01-PLAN.md
files_modified:
  - tests/project-state-service.test.ts
  - tests/cli-commands.test.ts
autonomous: true
requirements:
  - REVW-03
must_haves:
  truths:
    - "`status`, `frontier`, and `inspect` converge on the same post-decision state after manual review."
    - "Manual review does not rely on stale frontier snapshots when read models are queried after accept or reject."
  artifacts:
    - path: tests/project-state-service.test.ts
      provides: "Read-model consistency checks after manual accept and reject."
    - path: tests/cli-commands.test.ts
      provides: "CLI-level assertions that manual review leaves user-facing surfaces aligned."
  key_links:
    - "The shared frontier materializer is the authority for post-decision frontier state."
    - "Manual review should not leave `status`, `frontier`, and `inspect` disagreeing about the latest run."
---

<objective>
Prove that the post-decision read models stay aligned after manual review across both accept and reject outcomes.

Purpose: Close the user-visible trust gap by verifying that every surface the operator reads after manual review tells the same story.
Output: Regression coverage for `status`, `frontier`, and `inspect` after manual review, including snapshot repair behavior where relevant.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-manual-review-semantic-unification/04-CONTEXT.md
@src/app/services/project-state-service.ts
@src/app/services/manual-decision-service.ts
@tests/project-state-service.test.ts
@tests/cli-commands.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add read-model consistency coverage after manual review</name>
  <files>tests/project-state-service.test.ts
tests/cli-commands.test.ts</files>
  <read_first>tests/project-state-service.test.ts
tests/cli-commands.test.ts
src/app/services/project-state-service.ts
src/app/services/manual-decision-service.ts</read_first>
  <behavior>
    - Test 1: After manual accept, `status`, `frontier`, and `inspect` all agree on the latest run status and frontier membership.
    - Test 2: After manual reject, `status`, `frontier`, and `inspect` all agree that the run is rejected and frontier membership is unchanged.
    - Test 3: If frontier snapshots are stale before manual review completes, the post-review read path still converges on the materialized truth.
  </behavior>
  <action>Add read-model and CLI integration tests that query the public surfaces after manual review, using real stores and repos so the materializer path is exercised.</action>
  <verify>
    <automated>npx vitest run tests/project-state-service.test.ts tests/cli-commands.test.ts</automated>
  </verify>
  <done>There is automated proof that the post-review user-facing read models stay aligned.</done>
</task>

</tasks>

<success_criteria>
- `status`, `frontier`, and `inspect` agree after manual accept.
- `status`, `frontier`, and `inspect` agree after manual reject.
- Snapshot staleness does not reintroduce manual-review inconsistency.
</success_criteria>

<output>
After completion, create `.planning/phases/04-manual-review-semantic-unification/04-manual-review-semantic-unification-02-SUMMARY.md`
</output>
