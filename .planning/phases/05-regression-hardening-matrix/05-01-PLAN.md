---
phase: 05-regression-hardening-matrix
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/run-cycle-service.test.ts
  - tests/state-engines.test.ts
autonomous: true
requirements:
  - STAB-03
must_haves:
  truths:
    - "Same-run resume remains protected across later accepted-path checkpoints."
    - "Recovery classification and service-level resume behavior stay aligned."
  artifacts:
    - path: tests/run-cycle-service.test.ts
      provides: "Service-level resume coverage for additional accepted-path interruption boundaries."
    - path: tests/state-engines.test.ts
      provides: "Checkpoint classification coverage that matches the resumed service behavior."
  key_links:
    - "Resume hardening should test more than the `decision_written` boundary."
---

<objective>
Expand regression coverage for same-`runId` resume so later accepted-path interruption boundaries remain protected.

Purpose: Prevent future changes from silently breaking resumability after commit or frontier persistence has already happened.
Output: Additional service and state-engine regression tests for accepted-path checkpoints.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-regression-hardening-matrix/05-CONTEXT.md
@src/app/services/run-cycle-service.ts
@src/core/state/recovery-classifier.ts
@tests/run-cycle-service.test.ts
@tests/state-engines.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add resume-boundary regression matrix</name>
  <files>tests/run-cycle-service.test.ts
tests/state-engines.test.ts</files>
  <read_first>tests/run-cycle-service.test.ts
tests/state-engines.test.ts
src/app/services/run-cycle-service.ts
src/core/state/recovery-classifier.ts</read_first>
  <behavior>
    - Test 1: A `committed` latest run resumes on the same `runId`, updates frontier state, and completes.
    - Test 2: A `frontier_updated` latest run resumes on the same `runId`, cleans up the workspace, and completes.
    - Test 3: Recovery classification stays truthful for the added checkpoints.
  </behavior>
  <action>Add service-level and state-engine regression tests for the later accepted-path recovery boundaries, using seeded durable records and real repos.</action>
  <verify>
    <automated>npx vitest run tests/run-cycle-service.test.ts tests/state-engines.test.ts</automated>
  </verify>
  <done>The resume matrix explicitly protects the accepted-path checkpoints beyond `decision_written`.</done>
</task>

</tasks>

<success_criteria>
- Later accepted-path resume checkpoints are explicitly covered.
- Service-level resume and recovery classification remain aligned.
</success_criteria>

<output>
After completion, create `.planning/phases/05-regression-hardening-matrix/05-regression-hardening-matrix-01-SUMMARY.md`
</output>
