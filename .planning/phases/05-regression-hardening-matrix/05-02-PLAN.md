---
phase: 05-regression-hardening-matrix
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/cli-commands.test.ts
  - tests/validate-command.test.ts
  - tests/run-cycle-service.test.ts
autonomous: true
requirements:
  - STAB-04
must_haves:
  truths:
    - "Manifest/runtime contract rejection stays consistent across validate, doctor, and run surfaces."
  artifacts:
    - path: tests/cli-commands.test.ts
      provides: "CLI admission parity coverage."
    - path: tests/validate-command.test.ts
      provides: "Validation-surface contract regression coverage."
  key_links:
    - "Admission hardening is a matrix problem, not a single-surface check."
---

<objective>
Protect manifest admission truth with a parity matrix across the public contract-enforcement surfaces.

Purpose: Catch drift where one surface accepts or explains a manifest/runtime combination differently from the others.
Output: Regression tests for unsupported workspace, unsupported proposer, and baseline mismatch parity.
</objective>

<execution_context>
@/Users/chsong/.codex/get-shit-done/workflows/execute-plan.md
@/Users/chsong/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-regression-hardening-matrix/05-CONTEXT.md
@tests/cli-commands.test.ts
@tests/validate-command.test.ts
@tests/run-cycle-service.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract-enforcement parity matrix coverage</name>
  <files>tests/cli-commands.test.ts
tests/validate-command.test.ts
tests/run-cycle-service.test.ts</files>
  <read_first>tests/cli-commands.test.ts
tests/validate-command.test.ts
tests/run-cycle-service.test.ts</read_first>
  <behavior>
    - Test 1: Unsupported workspace manifests stay rejected across doctor, validate, and run.
    - Test 2: Unsupported `operator_llm` manifests stay rejected across doctor, validate, and run.
    - Test 3: Baseline mismatch keeps admission truth aligned where applicable.
  </behavior>
  <action>Extend the current admission tests into an explicit parity matrix so contract drift between surfaces becomes an immediate regression.</action>
  <verify>
    <automated>npx vitest run tests/cli-commands.test.ts tests/validate-command.test.ts tests/run-cycle-service.test.ts</automated>
  </verify>
  <done>Contract enforcement drift across the admission surfaces is directly tested.</done>
</task>

</tasks>

<success_criteria>
- Unsupported manifest/runtime combinations are protected by cross-surface parity tests.
- The admission contract cannot silently diverge between validate, doctor, and run.
</success_criteria>

<output>
After completion, create `.planning/phases/05-regression-hardening-matrix/05-regression-hardening-matrix-02-SUMMARY.md`
</output>
